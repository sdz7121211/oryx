# These are really here just to satisfy the framework in tests that don't specify any config.
model=${als-model}
model.local-computation=true
model.local-data=true
model.instance-dir=/tmp/OryxTest
test.integration=true